{
  "name": "tday",
  "version": "0.6.15",
  "private": true,
  "description": "The Ultimate Harness Agent Launcher",
  "author": "unbug",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:app": "corepack pnpm --filter @tday/desktop dev",
    "build": "corepack pnpm --filter @tday/desktop build",
    "package": "corepack pnpm --filter @tday/desktop build && corepack pnpm --filter @tday/desktop package:mac",
    "package:mac": "corepack pnpm --filter @tday/desktop build && corepack pnpm --filter @tday/desktop package:mac",
    "package:win": "corepack pnpm --filter @tday/desktop build && corepack pnpm --filter @tday/desktop package:win",
    "package:linux": "corepack pnpm --filter @tday/desktop build && corepack pnpm --filter @tday/desktop package:linux",
    "release": "node scripts/release.mjs",
    "build:core": "cd crates/tday-core && cargo build --release",
    "typecheck": "corepack pnpm -r --parallel typecheck",
    "lint": "corepack pnpm -r --parallel lint"
  },
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "esbuild",
      "node-pty"
    ]
  },
  "devDependencies": {
    "eslint": "^10.3.0",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^7.1.1",
    "typescript-eslint": "^8.59.1"
  }
}
